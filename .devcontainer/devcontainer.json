// The Dev Container format allows you to configure your environment. At the heart of it
// is a Docker image or Dockerfile which controls the tools available in your environment.
//
// See https://aka.ms/devcontainer.json for more information.
{
	"name": "Gitpod",
	// Use "image": "mcr.microsoft.com/devcontainers/base:ubuntu-24.04",
	// instead of the build to use a pre-built image.
	"build": {
        "context": ".",
        "dockerfile": "Dockerfile"
        // "args": { "MYARG": "MYVALUE", "MYVAR=VALUE" },
        // "options": [
        //     "--platform=linux/arm64"
        // ]
    },
    "containerUser": "rstudio",
    "workspaceMount": "source=${localWorkspaceFolder},target=/home/rstudio/${localWorkspaceFolderBasename},type=bind,consistency=cached",
    "workspaceFolder": "/home/rstudio/${localWorkspaceFolderBasename}",
    "initializeCommand": "env | xargs"
    // "onCreateCommand": "echo rstudio:$PASSWORD | chpasswd && chown -R rstudio /home/rstudio/"
    // "containerEnv": { "PASSWORD": "${containerEnv:PASSWORD}"
    // },
    // "runArgs": [
    //     "--mount", "type=bind,src=/workspaces/mack_d_2023.04.13_iPSC_neuromuscular_scRNAseq,dst=/home/rstudio/mack_d_2023.04.13_iPSC_neuromuscular_scRNAseq"
    //     "--env", "ROOT=true"
    // ],
	// Features add additional features to your environment. See https://containers.dev/features
	// Beware: features are not supported on all platforms and may have unintended side-effects.
	// "features": {
    //   "ghcr.io/devcontainers/features/docker-in-docker": {
    //     "moby": false
    //   }
    // }
}
